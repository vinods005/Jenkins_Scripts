pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'Build stage'
            }
        }
        stage('Deploy') {
            steps {
                sh 'date'
            }
        }
    }
}
